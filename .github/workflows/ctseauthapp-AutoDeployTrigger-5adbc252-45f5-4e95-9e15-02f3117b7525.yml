name: Trigger auto deployment for ctseauthapp

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/ctseauthapp-AutoDeployTrigger-5adbc252-45f5-4e95-9e15-02f3117b7525.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.CTSEAUTHAPP_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CTSEAUTHAPP_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CTSEAUTHAPP_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: ctseauthcontainer.azurecr.io
          registryUsername: ${{ secrets.CTSEAUTHAPP_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CTSEAUTHAPP_REGISTRY_PASSWORD }}
          containerAppName: ctseauthapp
          resourceGroup: CTSE_GROUP
          imageToBuild: ctseauthcontainer.azurecr.io/ctseauthapp:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


